import "../styles.scss";

const NotFoundState = (): React.ReactElement => {
  return (
    <section className="state">
      <div>
        <h2 className="mb-4 text-6xl font-extrabold">
          Page not found
        </h2>
      </div>
    </section>
  );
};

export default NotFoundState;
